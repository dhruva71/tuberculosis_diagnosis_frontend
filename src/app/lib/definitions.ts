export type XrayData = {
    id: number;
    class: string;
    timestamp: string;
};

export type User = {
    id: number;
    name: string;
    email: string;
    image: string;
    password: string;
};